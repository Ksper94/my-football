const nextI18NextConfig = {
  i18n: {
    locales: ['fr', 'en', 'es'], // Langues supportées
    defaultLocale: 'fr', // Langue par défaut
    localeDetection: true, // Activer la détection automatique
  },
};

export default nextI18NextConfig;
